# .venv-train  — WORKSTATION ONLY.
#
# Install torch FIRST, from the CUDA 12.8+ index, or Blackwell (sm_120) will
# not work:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-train.txt
#
# Do NOT add qai-hub packages here. They pin their own torch.

rfdetr[onnx,train,loggers]  # [train] pulls in pytorch-lightning — rfdetr 1.9.x trains via a
                            # PTL Trainer under the hood; without this extra,
                            # `from rfdetr.training import ...` raises ModuleNotFoundError.
supervision
opencv-python
pillow
numpy
pandas
pyyaml
python-dotenv
wandb
onnx
onnxruntime
huggingface-hub
